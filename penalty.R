#--------------------------------------------------------------------------------------
#
# penalty term
#
#--------------------------------------------------------------------------------------
penalty <- function(x,alpha=0.05,w=1,rs0=2) {
  a <- x**(10*w)
  b <- rs0**(10*w)
  tf <- a/(a+b)
  tfa <- alpha * tf
  return(tfa)
}
