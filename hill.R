#--------------------------------------------------------------------------------------
#
# calculate a Hill function
#
#--------------------------------------------------------------------------------------
hill <- function(x,mid,wid,bot=0,top=1) {
  rng <- top-bot
  val <- bot + rng*(x**wid/(x**wid + mid**wid))
  return(val)
}
